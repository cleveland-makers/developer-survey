import React from 'react';

const styles = {
  div: {
    margin: 'auto',
    width: '60%',
  },
};

const Loading = () => (
  <section style={styles.div}>
    <p>
      Loading...
    </p>
  </section>
);

export default Loading;
